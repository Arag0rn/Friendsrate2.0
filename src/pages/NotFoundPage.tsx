import { NotFound } from "../components/NotFound";

export const NotFoundPage = () => <NotFound />;

// 404 File not found The site configured at this address does not contain
// the requested file. If this is your site, make sure that the filename case
// matches the URL as well as any file permissions. For root URLs (like
// http://example.com/) you must provide an index.html file.
